# 檢查啟動錯誤 - 立即執行

> **日期**: 2025-12-01

---

## 🔍 立即檢查

執行以下命令查看啟動錯誤：

```bash
# 查看啟動日誌
echo "=== 後端啟動日誌 ==="
tail -50 /tmp/backend.log

# 檢查是否有其他進程
echo ""
echo "=== 檢查端口 8000 ==="
lsof -i :8000 || netstat -tlnp | grep :8000

# 檢查後端進程
echo ""
echo "=== 檢查後端進程 ==="
ps aux | grep uvicorn | grep -v grep
```

---

**執行這個命令，告訴我日誌內容，我會幫您解決問題！**
