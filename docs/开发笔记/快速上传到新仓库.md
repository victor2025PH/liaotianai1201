# 快速上傳到新倉庫

> **目標倉庫**: https://github.com/victor2025PH/liaotianai1201.git

---

## 🚀 三步驟上傳

### 步驟 1: 設置遠程倉庫

```powershell
# 添加或更新遠程倉庫地址
git remote add origin https://github.com/victor2025PH/liaotianai1201.git
# 或更新現有遠程：
git remote set-url origin https://github.com/victor2025PH/liaotianai1201.git

# 確認遠程倉庫設置
git remote -v
```

### 步驟 2: 確保所有文件已提交

```powershell
# 查看狀態
git status

# 添加所有文件（.gitignore 會自動排除不應提交的文件）
git add .

# 提交（如果需要）
git commit -m "chore: 上傳項目到新倉庫"
```

### 步驟 3: 推送到遠程

```powershell
# 推送到 main 分支
git push -u origin main

# 如果本地分支不是 main，推送到當前分支：
git push -u origin $(git branch --show-current)
```

---

## ✅ 驗證上傳

訪問倉庫頁面確認：
```
https://github.com/victor2025PH/liaotianai1201
```

---

## 🔐 如果遇到認證問題

### 方法 1: 使用 GitHub CLI（推薦）

```powershell
# 安裝 GitHub CLI（如果還沒有）
# winget install GitHub.cli

# 登錄
gh auth login
```

### 方法 2: 使用 Personal Access Token

1. 生成 Token: https://github.com/settings/tokens
2. 配置 Git:

```powershell
git config --global credential.helper manager-core
# 推送時會提示輸入用戶名和 Token（不是密碼）
```

---

**完成！** 🎉
