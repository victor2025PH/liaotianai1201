# 立即測試 - 最簡單方法

> **日期**: 2025-12-01

---

## 🎯 最簡單的方法

先檢查後端是否還在運行，如果還在運行，直接測試；如果已停止，嘗試重啟。

---

## 📋 執行以下命令

```bash
# 步驟 1: 檢查後端是否在運行
echo "檢查後端服務狀態..."
if curl -s http://localhost:8000/health 2>/dev/null | grep -q "ok"; then
    echo "✅ 後端服務正在運行"
    echo ""
    echo "直接測試修復..."
    echo ""
    
    # 獲取 Token 並測試
    TOKEN=$(curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
      -H "Content-Type: application/x-www-form-urlencoded" \
      -d "username=admin@example.com&password=changeme123" | \
      python3 -c "import sys, json; print(json.loads(sys.stdin.read()).get('access_token', ''))" 2>/dev/null)
    
    if [ -n "$TOKEN" ]; then
        echo "HTTP狀態碼:"
        curl -s -o /tmp/status.json -w "%{http_code}\n" \
          -H "Authorization: Bearer $TOKEN" \
          "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status"
        
        echo ""
        echo "響應內容:"
        python3 -m json.tool /tmp/status.json 2>/dev/null || cat /tmp/status.json
    else
        echo "❌ 無法獲取 Token"
    fi
else
    echo "❌ 後端服務已停止"
    echo ""
    echo "需要重啟服務。請執行以下命令找到目錄："
    echo ""
    echo "find ~ -type d -name 'admin-backend' 2>/dev/null | head -3"
    echo "find /opt -type d -name 'admin-backend' 2>/dev/null | head -3"
fi
```

---

## 🚀 或者更簡單：直接測試

如果後端之前能運行，可能已經自動重啟了。直接測試：

```bash
TOKEN=$(curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=changeme123" | \
  python3 -c "import sys, json; d=json.load(sys.stdin); print(d.get('access_token',''))" 2>/dev/null)

if [ -n "$TOKEN" ]; then
    echo "=== 測試賬號狀態端點 ==="
    curl -s -o /tmp/test_response.json -w "HTTP狀態碼: %{http_code}\n" \
      -H "Authorization: Bearer $TOKEN" \
      "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status"
    echo ""
    echo "響應:"
    python3 -m json.tool /tmp/test_response.json 2>/dev/null || cat /tmp/test_response.json
else
    echo "無法獲取Token，後端服務可能已停止"
    echo "檢查服務狀態:"
    curl http://localhost:8000/health
fi
```

---

**執行最後這個簡單測試命令，告訴我結果！**
