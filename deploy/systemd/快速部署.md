# 快速部署指南

## 🚀 一键部署（推荐）

### 从 Windows 本地部署到 Linux 服务器

```powershell
# 1. 进入部署目录
cd deploy/systemd

# 2. 执行自动部署脚本
.\auto_deploy_remote.ps1 `
    -ServerIP "your-server-ip" `
    -Username "root" `
    -Password "your-password" `
    -ProjectPath "/opt/smart-tg"
```

### 示例

```powershell
.\auto_deploy_remote.ps1 `
    -ServerIP "165.154.254.99" `
    -Username "root" `
    -Password "Along2025!!!" `
    -ProjectPath "/opt/smart-tg"
```

---

## 📋 部署前准备

### 1. 确保代码已上传到服务器

如果代码还未上传，可以使用以下方式：

#### 方式 A: 使用 SCP 上传

```powershell
# 上传后端代码
scp -r admin-backend/* root@your-server-ip:/opt/smart-tg/admin-backend/

# 上传前端代码
scp -r saas-demo/* root@your-server-ip:/opt/smart-tg/saas-demo/
```

#### 方式 B: 使用 Git（如果服务器有 Git）

```bash
# 在服务器上
cd /opt/smart-tg
git clone your-repo-url .
```

### 2. 配置环境变量

在服务器上编辑环境变量文件：

```bash
# 后端环境变量
nano /opt/smart-tg/admin-backend/.env

# 前端环境变量
nano /opt/smart-tg/saas-demo/.env.local
```

---

## 🔍 部署后检查

### 自动检查

部署脚本会自动检查部署状态。

### 手动检查

```bash
# SSH 连接到服务器
ssh root@your-server-ip

# 检查服务状态
sudo systemctl status smart-tg-backend
sudo systemctl status smart-tg-frontend

# 检查健康端点
curl http://localhost:8000/health
curl http://localhost:3000

# 查看日志
sudo journalctl -u smart-tg-backend -f
sudo journalctl -u smart-tg-frontend -f
```

---

## 🐛 常见问题

### 问题 1: 连接失败

**解决方案:**
- 检查服务器 IP 和密码是否正确
- 检查防火墙是否开放 SSH 端口（22）
- 尝试手动 SSH 连接测试

### 问题 2: 代码未上传

**解决方案:**
- 先使用 SCP 上传代码
- 或使用 Git 在服务器上克隆代码
- 然后重新运行部署脚本

### 问题 3: 服务启动失败

**解决方案:**
```bash
# 查看详细日志
sudo journalctl -u smart-tg-backend -n 50
sudo journalctl -u smart-tg-frontend -n 50

# 检查权限
sudo chown -R www-data:www-data /opt/smart-tg
```

---

*快速部署指南最后更新: 2025-11-26*

