# E2E 測試修復說明（服務器執行）

> **日期**: 2025-01-28

---

## ⚠️ 重要：需要同步文件

服務器上的測試文件需要更新！錯誤顯示文件還在用舊的導入方式。

---

## 🔧 修復步驟

### 1. 確認在正確目錄

```bash
cd /home/ubuntu/liaotian/saas-demo
# 或
cd ~/liaotian/saas-demo
```

### 2. 拉取最新代碼（如果使用 Git）

```bash
git pull origin master
```

### 3. 或手動檢查文件

檢查測試文件是否已更新：

```bash
# 檢查導入語句
head -n 3 e2e/dashboard.spec.ts
# 應該顯示: import { test, expect } from '@playwright/test';
```

### 4. 確認輔助文件存在

```bash
# 檢查認證 helper
ls -la e2e/helpers/auth.ts

# 如果不存在，需要創建
```

### 5. 運行測試

```bash
npm run test:e2e
```

---

## 📝 修復內容總結

### 所有測試文件現在使用：

```typescript
import { test, expect } from '@playwright/test';
import { ensureLoggedIn } from './helpers/auth';
```

### 每個測試都有：

```typescript
test.beforeEach(async ({ page }) => {
  await ensureLoggedIn(page);
});
```

---

## ✅ 應該解決的問題

- ❌ `Error: Playwright Test did not expect test.describe() to be called here.`
- ✅ 測試文件可以正常識別和運行

---

**狀態**: 等待在服務器上驗證  
**下一步**: 同步文件後重新運行測試
