# 502 錯誤 - 啟動前端服務

> **日期**: 2025-12-01  
> **問題**: 前端服務未運行，需要啟動

---

## 🔍 當前狀況

- ❌ 前端進程未找到
- ❌ 端口 3000 未監聽
- ❌ 通過 Nginx 訪問返回 502

---

## 🔧 解決方案

### 步驟 1: 查找前端項目

```bash
echo "=== 查找前端項目 ===" && \
echo "" && \
echo "【1】查找 package.json 文件..." && \
find /home/ubuntu/liaotian -name "package.json" -type f 2>/dev/null | head -10 && \
echo "" && \
echo "【2】查找 Next.js 項目..." && \
find /home/ubuntu/liaotian -name "next.config.*" -type f 2>/dev/null | head -5 && \
echo "" && \
echo "【3】檢查常見的前端目錄..." && \
ls -la /home/ubuntu/liaotian/ 2>/dev/null | grep -E "saas|frontend|web|app" && \
ls -la /home/ubuntu/liaotian/deployment-package/ 2>/dev/null | grep -E "saas|frontend|web|app"
```

### 步驟 2: 啟動前端服務

找到前端項目後，執行：

```bash
# 替換 <前端項目路徑> 為實際路徑
cd <前端項目路徑> && \
echo "安裝依賴（如果需要）..." && \
npm install 2>&1 | tail -5 && \
echo "" && \
echo "啟動前端服務..." && \
nohup npm run dev > /tmp/frontend.log 2>&1 & \
sleep 10 && \
echo "檢查服務狀態..." && \
if lsof -i :3000 > /dev/null 2>&1; then
    echo "✅ 前端服務已啟動"
    curl -s http://127.0.0.1:3000 | head -3
else
    echo "❌ 前端服務啟動失敗"
    echo "查看日誌:"
    tail -50 /tmp/frontend.log
fi
```

---

**先執行步驟 1 查找前端項目，然後根據結果啟動服務！**
