# 手动触发 GitHub Actions 部署

## 🔧 方法 1：通过 GitHub 网页手动触发（最简单）

### 步骤：

1. **访问 GitHub Actions 页面**
   ```
   https://github.com/victor2025PH/liaotianai1201/actions
   ```

2. **找到 "Deploy to Server" 工作流**
   - 在左侧工作流列表中，点击 "Deploy to Server"

3. **点击 "Run workflow" 按钮**
   - 在工作流详情页面的右侧，找到并点击 **"Run workflow"** 按钮
   - 选择分支：`main`
   - 点击绿色的 **"Run workflow"** 按钮

4. **查看部署状态**
   - 页面会刷新，显示新的工作流运行
   - 点击进入查看详细日志

---

## 🔧 方法 2：通过推送代码触发

如果您想通过代码推送触发，可以做一个小的改动：

```bash
# 在本地项目目录
cd D:\telegram-ai-system

# 做一个小的改动（例如在 README 中添加一个空行）
echo "" >> README.md

# 提交并推送
git add README.md
git commit -m "trigger: 触发部署测试"
git push origin main
```

---

## 🔧 方法 3：使用 GitHub CLI（如果已安装）

```bash
gh workflow run "Deploy to Server" --ref main
```

---

## 📋 工作流触发条件

根据 `.github/workflows/deploy.yml` 配置，工作流会在以下情况触发：

1. **自动触发：**
   - ✅ Push 到 `main` 分支时
   - ✅ 应该已经触发了（如果刚才的提交推送到了 main 分支）

2. **手动触发：**
   - ✅ `workflow_dispatch`（可以通过 GitHub 网页手动触发）

---

## ❓ 如果没有自动触发

可能的原因：

1. **提交可能没有推送到 main 分支**
   ```bash
   # 检查当前分支
   git branch
   
   # 确认推送成功
   git log origin/main -1
   ```

2. **工作流文件可能有语法错误**
   - GitHub Actions 页面会显示错误
   - 检查是否有红色错误提示

3. **Actions 可能被禁用**
   - 检查仓库设置 > Actions > General
   - 确保 Actions 已启用

---

## ✅ 推荐操作

**立即执行：**

1. 访问：`https://github.com/victor2025PH/liaotianai1201/actions`
2. 点击左侧的 "Deploy to Server" 工作流
3. 点击右侧的 "Run workflow" 按钮
4. 选择 `main` 分支
5. 点击绿色的 "Run workflow"

这样就可以立即触发部署了！
