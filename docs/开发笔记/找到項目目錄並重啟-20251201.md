# 找到項目目錄並重啟後端

> **日期**: 2025-12-01  
> **問題**: 後端服務已停止，需要找到項目目錄並重啟

---

## 🔍 第一步：找到項目目錄

執行以下命令查找後端目錄：

```bash
# 方法1: 查找 accounts.py 文件
find / -name "accounts.py" -path "*/group_ai/*" 2>/dev/null | head -1

# 方法2: 查找 admin-backend 目錄
find / -type d -name "admin-backend" 2>/dev/null | head -3

# 方法3: 查找 main.py
find / -name "main.py" -path "*/app/main.py" 2>/dev/null | head -1

# 方法4: 查看當前運行的進程的工作目錄（如果有舊進程）
ps aux | grep uvicorn | grep -v grep
```

---

## 🚀 第二步：重啟後端

找到目錄後，進入目錄並重啟：

```bash
# 根據找到的路徑調整（假設找到的是 /path/to/admin-backend）
cd /path/to/admin-backend

# 激活虛擬環境（如果存在）
source ../.venv/bin/activate 2>/dev/null || source .venv/bin/activate 2>/dev/null || source venv/bin/activate 2>/dev/null

# 啟動服務
nohup python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &
sleep 5

# 檢查啟動狀態
tail -20 /tmp/backend.log
curl http://localhost:8000/health
```

---

## 🧪 第三步：測試

```bash
TOKEN=$(curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=changeme123" | \
  python3 -c "import sys, json; print(json.loads(sys.stdin.read()).get('access_token', ''))") && \
HTTP_CODE=$(curl -s -o /tmp/status.json -w "%{http_code}" \
  -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status") && \
echo "HTTP狀態碼: $HTTP_CODE" && \
echo "" && \
echo "響應內容:" && \
python3 -m json.tool /tmp/status.json
```

---

**先執行第一步找到項目目錄，告訴我結果！**
