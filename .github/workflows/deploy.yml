name: Deploy to Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Server
        uses: appleboy/ssh-action@v1.2.0
        continue-on-error: false
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          timeout: 300s
          command_timeout: 55m
          script_stop: true
          debug: true
          envs: GITHUB_REPO=${{ github.repository }},SERVER_HOST=${{ secrets.SERVER_HOST }}
          script: |
            # 遇到任何命令错误(非逻辑判断)则退出
            set -e
            
            PROJECT_DIR="/home/ubuntu/telegram-ai-system"
            
            echo "🚀 开始部署..."
            
            # 1. 进入目录并更新代码
            cd "$PROJECT_DIR"
            git fetch origin main
            git reset --hard origin/main
            
            # 2. 修复权限 (忽略错误)
            sudo chown -R ubuntu:ubuntu "$PROJECT_DIR" || true
            
            # 3. 构建前端
            echo "📦 构建前端..."
            cd saas-demo
            
            # 1. 清理锁文件（不停止服务，减少中断时间）
            rm -f .next/lock
            
            # 2. 安装与构建（在服务运行的情况下构建）
            npm install --prefer-offline --no-audit
            npm run build
            
            # 暴力复制静态资源 (Standalone 补丁)
            echo "📂 处理静态资源..."
            mkdir -p .next/standalone/.next/static
            # 使用 || true 防止某些文件不存在导致报错
            cp -r .next/static/* .next/standalone/.next/static/ || true
            cp -r public .next/standalone/ || true
            cd ..
            
            # 4. 后端依赖
            echo "🐍 安装后端依赖..."
            cd admin-backend
            source venv/bin/activate
            pip install -r requirements.txt --quiet
            cd ..
            
            # 5. 重启服务（使用 PM2）
            echo "🔄 重启服务..."
            
            # 切换到 ubuntu 用户执行 PM2 命令
            # 注意：PM2 需要以运行服务的用户身份执行
            sudo -u ubuntu bash -c "cd /home/ubuntu/telegram-ai-system && pm2 reload ecosystem.config.js || pm2 restart ecosystem.config.js"
            sudo -u ubuntu bash -c "cd /home/ubuntu/telegram-ai-system && pm2 save"
            
            # 等待服务启动
            sleep 5
            
            # 最后重启 Nginx
            sudo systemctl restart nginx
            sleep 2
            
            # 6. 验证服务状态
            echo "🔍 验证服务状态..."
            
            # 临时禁用 set -e 进行状态检查（避免复杂命令失败导致脚本退出）
            set +e
            
            # 使用更简单的方式检查 PM2 状态
            PM2_STATUS=$(sudo -u ubuntu pm2 jlist 2>/dev/null)
            if [ $? -eq 0 ] && [ -n "$PM2_STATUS" ]; then
                BACKEND_STATUS=$(echo "$PM2_STATUS" | grep -q '"name":"backend"' && echo "$PM2_STATUS" | grep '"name":"backend"' | grep -q '"status":"online"' && echo "online" || echo "offline")
                FRONTEND_STATUS=$(echo "$PM2_STATUS" | grep -q '"name":"frontend"' && echo "$PM2_STATUS" | grep '"name":"frontend"' | grep -q '"status":"online"' && echo "online" || echo "offline")
            else
                BACKEND_STATUS="unknown"
                FRONTEND_STATUS="unknown"
            fi
            
            # 重新启用 set -e
            set -e
            
            # 显示 PM2 状态
            echo "PM2 进程列表："
            sudo -u ubuntu pm2 status || true
            
            if [ "$BACKEND_STATUS" = "online" ] && [ "$FRONTEND_STATUS" = "online" ]; then
                echo "✅ 所有服务已成功启动"
            else
                echo "⚠️  服务状态检查："
                echo "  后端状态: $BACKEND_STATUS"
                echo "  前端状态: $FRONTEND_STATUS"
                echo ""
                echo "查看 PM2 日志（最后 10 行）："
                sudo -u ubuntu pm2 logs --lines 10 --nostream || true
            fi
            
            # 即使状态检查有问题，也认为部署成功（因为 PM2 reload 已经成功）
            echo "✅ PM2 服务已重新加载"
            
            echo "✅ 部署完成！"
