# 完整功能測試 - 立即執行

> **日期**: 2025-12-01  
> **任務**: 完整功能測試

---

## 🚀 立即執行測試

在服務器終端執行以下完整測試：

```bash
# 獲取 Token
TOKEN=$(curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=changeme123" | \
  python3 -c "import sys, json; print(json.loads(sys.stdin.read()).get('access_token', ''))")

echo "=========================================" && \
echo "完整功能測試" && \
echo "=========================================" && \
echo "" && \
echo "【測試 1】查看賬號詳情..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591" | \
  python3 -m json.tool 2>/dev/null | head -50 || \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591" && \
echo "" && \
echo "" && \
echo "【測試 2】查看賬號狀態..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status" | \
  python3 -m json.tool 2>/dev/null || \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status" && \
echo "" && \
echo "" && \
echo "【測試 3】查看劇本詳情..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/scripts/$(python3 -c "import urllib.parse; print(urllib.parse.quote('紅包遊戲陪玩劇本'))")" | \
  python3 -m json.tool 2>/dev/null | head -50 || \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/scripts/紅包遊戲陪玩劇本" | head -30 && \
echo "" && \
echo "" && \
echo "【測試 4】查看 Worker 命令..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/workers/computer_001/commands" | \
  python3 -m json.tool 2>/dev/null | head -30 || \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/workers/computer_001/commands" && \
echo "" && \
echo "" && \
echo "【測試 5】查看監控系統指標..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/monitor/system" | \
  python3 -m json.tool 2>/dev/null | head -40 || \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/monitor/system" && \
echo "" && \
echo "=========================================" && \
echo "測試完成" && \
echo "========================================="
```

---

**執行測試命令，告訴我結果！**
