name: 自動修復代碼格式

on:
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  auto-fix:
    name: 自動修復格式問題
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: 檢出代碼
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 設置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'poetry'
      
      - name: 安裝 Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
      
      - name: 安裝依賴
        working-directory: admin-backend
        run: poetry install --no-interaction --no-root
      
      - name: 自動修復 Python 代碼格式（Ruff）
        working-directory: admin-backend
        run: |
          poetry run ruff check --fix . || true
      
      - name: 自動修復 Python 代碼格式（Black）
        working-directory: admin-backend
        run: |
          poetry run black . || true
      
      - name: 設置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: saas-demo/package-lock.json
      
      - name: 自動修復前端代碼格式
        working-directory: saas-demo
        run: |
          npm ci
          npm run lint -- --fix || true
      
      - name: 檢查是否有變更
        id: check-changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi
      
      - name: 提交修復
        if: steps.check-changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          git commit -m "🔧 自動修復代碼格式問題 [skip ci]" || exit 0
          git push || exit 0
      
      - name: 添加 PR 評論
        if: steps.check-changes.outputs.has_changes == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ 已自動修復代碼格式問題，請查看最新的提交。'
            })
