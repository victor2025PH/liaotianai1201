# 現在執行 - 最簡單的方案

> **日期**: 2025-12-01

---

## 🚀 立即執行（複製貼上）

### 方案 A: 如果後端還在運行（最可能）

```bash
curl -s http://localhost:8000/health && echo "" && \
TOKEN=$(curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=changeme123" | \
  python3 -c "import sys, json; d=json.load(sys.stdin); print(d.get('access_token',''))" 2>/dev/null) && \
echo "測試賬號狀態端點:" && \
curl -s -o /tmp/test.json -w "HTTP狀態碼: %{http_code}\n" \
  -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status" && \
echo "" && echo "響應:" && \
python3 -m json.tool /tmp/test.json 2>/dev/null || cat /tmp/test.json
```

---

### 方案 B: 如果後端已停止，先查找目錄

```bash
# 查找項目目錄
echo "查找項目目錄..."
find ~ -type d -name "admin-backend" -exec test -f {}/app/main.py \; -print 2>/dev/null | head -1
find /opt -type d -name "admin-backend" -exec test -f {}/app/main.py \; -print 2>/dev/null | head -1
```

找到目錄後，進入目錄並重啟：

```bash
cd <找到的路徑>
source ../.venv/bin/activate 2>/dev/null || source .venv/bin/activate 2>/dev/null
nohup python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &
sleep 5
curl http://localhost:8000/health
```

---

## 🎯 推薦執行順序

**第一步**: 先檢查後端是否還在運行
```bash
curl http://localhost:8000/health
```

**如果返回** `{"status":"ok"}` → **執行方案 A 直接測試**

**如果返回錯誤** → **執行方案 B 查找並重啟**

---

**先執行第一步檢查，告訴我結果！**
