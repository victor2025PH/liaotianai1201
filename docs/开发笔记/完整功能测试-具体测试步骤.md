# 完整功能測試 - 具體測試步驟

> **日期**: 2025-12-01  
> **任務**: 本週任務 2 - 完整功能測試

---

## 📋 測試概述

基於實際運行數據，進行端到端的功能測試。

---

## 🧪 測試項目

### 測試 1: 查看現有資源

**目標**: 確認當前系統中的資源

#### 1.1 查看劇本詳情

```bash
# 獲取 Token
TOKEN=$(curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=changeme123" | \
  python3 -c "import sys, json; print(json.loads(sys.stdin.read()).get('access_token', ''))")

# 查看劇本詳情（使用 script_id: "紅包遊戲陪玩劇本"）
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/scripts/紅包遊戲陪玩劇本" | \
  python3 -m json.tool | head -50
```

#### 1.2 查看賬號詳情

```bash
# 查看賬號詳情（account_id: "639454959591"）
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591" | \
  python3 -m json.tool | head -50
```

#### 1.3 查看 Worker 狀態

```bash
# 查看特定 Worker 的命令
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/workers/computer_001/commands" | \
  python3 -m json.tool | head -30
```

---

### 測試 2: 賬號操作流程

**目標**: 測試賬號的啟動和停止

#### 2.1 查看賬號狀態

```bash
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status" | \
  python3 -m json.tool
```

#### 2.2 啟動賬號（如果離線）

```bash
curl -s -X POST \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/start" | \
  python3 -m json.tool
```

#### 2.3 停止賬號（如果在線）

```bash
curl -s -X POST \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/stop" | \
  python3 -m json.tool
```

---

### 測試 3: 劇本管理流程

**目標**: 測試劇本的查看和操作

#### 3.1 查看劇本列表

```bash
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/scripts/" | \
  python3 -m json.tool | head -50
```

#### 3.2 查看劇本詳情

```bash
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/scripts/紅包遊戲陪玩劇本" | \
  python3 -m json.tool | head -100
```

---

### 測試 4: 監控和統計

**目標**: 測試監控功能

#### 4.1 查看監控指標

```bash
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/monitor/system" | \
  python3 -m json.tool | head -50
```

#### 4.2 查看賬號指標

```bash
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/monitor/accounts/metrics" | \
  python3 -m json.tool | head -50
```

---

## 🚀 快速測試命令

執行以下命令快速測試關鍵功能：

```bash
# 獲取 Token
TOKEN=$(curl -s -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin@example.com&password=changeme123" | \
  python3 -c "import sys, json; print(json.loads(sys.stdin.read()).get('access_token', ''))")

echo "=== 完整功能測試 ===" && \
echo "" && \
echo "【1】查看賬號狀態..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/accounts/639454959591/status" | \
  python3 -m json.tool && \
echo "" && \
echo "" && \
echo "【2】查看 Worker 命令..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/workers/computer_001/commands" | \
  python3 -m json.tool | head -20 && \
echo "" && \
echo "" && \
echo "【3】查看監控指標..." && \
curl -s -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8000/api/v1/group-ai/monitor/system" | \
  python3 -m json.tool | head -30 && \
echo "" && \
echo "=== 測試完成 ==="
```

---

**執行測試命令，告訴我結果！**
