# 上傳項目到新 GitHub 倉庫指南

> **日期**: 2025-11-30  
> **目標倉庫**: https://github.com/victor2025PH/liaotianai1201.git

---

## 🎯 目標

將當前項目完整上傳到新的 GitHub 倉庫。

---

## 📋 上傳前檢查清單

### ✅ 已確認配置

- [x] `.gitignore` 已正確配置，排除大文件目錄
- [x] 環境變量文件（`.env*`）已排除
- [x] 虛擬環境（`.venv/`）已排除
- [x] 依賴目錄（`node_modules/`）已排除
- [x] 運行數據（`backup/`, `sessions/`, `logs/`）已排除

### ⚠️ 需要確認

- [ ] 是否有敏感信息需要移除？
- [ ] 是否所有必要的文件都已包含？
- [ ] GitHub 認證是否已配置？

---

## 🚀 上傳步驟

### 方法 1: 使用自動化腳本（推薦）⭐

```powershell
# 執行上傳腳本
.\scripts\上傳到新倉庫.ps1
```

**腳本會自動執行以下步驟**：
1. ✅ 檢查 Git 倉庫狀態
2. ✅ 驗證 .gitignore 配置
3. ✅ 添加所有待提交的文件
4. ✅ 創建提交（如果需要）
5. ✅ 設置或更新遠程倉庫地址
6. ✅ 推送到新倉庫

### 方法 2: 手動執行命令

#### 步驟 1: 檢查 Git 狀態

```powershell
# 檢查當前狀態
git status

# 查看所有分支
git branch -a

# 查看提交歷史
git log --oneline -5
```

#### 步驟 2: 確保所有文件已提交

```powershell
# 查看待提交的文件
git status --short

# 添加所有文件（.gitignore 會自動排除不應提交的文件）
git add .

# 提交
git commit -m "chore: 準備上傳到新倉庫"
```

#### 步驟 3: 設置遠程倉庫

```powershell
# 檢查當前遠程倉庫
git remote -v

# 如果已有 origin，更新它
git remote set-url origin https://github.com/victor2025PH/liaotianai1201.git

# 如果沒有 origin，添加它
git remote add origin https://github.com/victor2025PH/liaotianai1201.git
```

#### 步驟 4: 推送到新倉庫

```powershell
# 推送到 main 分支（如果遠程倉庫是空的）
git push -u origin main

# 或推送到當前分支
git push -u origin $(git branch --show-current)
```

---

## 🔐 GitHub 認證配置

### 如果推送時遇到認證問題

#### 方法 1: 使用 Personal Access Token (PAT)

1. **生成 Token**
   - 訪問: https://github.com/settings/tokens
   - 點擊 "Generate new token (classic)"
   - 選擇權限：至少需要 `repo` 權限
   - 複製生成的 token

2. **配置 Git**

```powershell
# 方式 A: 使用 GitHub CLI（推薦）
gh auth login

# 方式 B: 使用 Git Credential Manager
git config --global credential.helper manager-core

# 方式 C: 在 URL 中包含 token（臨時使用）
git remote set-url origin https://<TOKEN>@github.com/victor2025PH/liaotianai1201.git
```

#### 方法 2: 使用 SSH

```powershell
# 1. 生成 SSH 密鑰（如果還沒有）
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. 將公鑰添加到 GitHub
#    複製 ~/.ssh/id_ed25519.pub 內容
#    添加到: https://github.com/settings/keys

# 3. 修改遠程倉庫 URL 為 SSH 格式
git remote set-url origin git@github.com:victor2025PH/liaotianai1201.git
```

---

## 📊 上傳內容確認

### 會被上傳的內容

- ✅ 所有源代碼文件（`.py`, `.ts`, `.tsx`, `.js` 等）
- ✅ 配置文件（`.yaml`, `.json`, `.toml` 等）
- ✅ 文檔文件（`.md`, `.txt` 等）
- ✅ 項目配置文件（`package.json`, `requirements.txt`, `pyproject.toml` 等）
- ✅ CI/CD 配置文件（`.github/workflows/`）
- ✅ 測試文件
- ✅ 數據庫遷移文件

### 不會上傳的內容（已排除）

- ❌ `.venv/` - Python 虛擬環境
- ❌ `node_modules/` - Node.js 依賴
- ❌ `__pycache__/` - Python 緩存
- ❌ `backup/` - 備份目錄
- ❌ `sessions/` - Session 文件
- ❌ `logs/` - 日誌文件
- ❌ `.env*` - 環境變量文件
- ❌ `无用文件/` - 已歸檔的無用文件
- ❌ 數據庫文件（`.db`, `.sqlite`）

**預計上傳大小**: 約 10-50 MB（主要是源代碼和文檔）

---

## 🔍 驗證上傳結果

### 1. 訪問倉庫頁面

```
https://github.com/victor2025PH/liaotianai1201
```

### 2. 檢查文件結構

確認以下關鍵文件/目錄已上傳：
- ✅ `README.md`
- ✅ `.github/workflows/`（CI/CD 配置）
- ✅ `admin-backend/`（後端代碼）
- ✅ `saas-demo/`（前端代碼）
- ✅ `docs/`（文檔）
- ✅ `.gitignore`

### 3. 檢查倉庫大小

- 倉庫應該只有約 10-50 MB
- 如果超過 100 MB，可能有遺漏的大文件

---

## 🐛 常見問題

### 問題 1: 推送時提示認證失敗

**解決方案**:
```powershell
# 檢查認證配置
git config --global user.name
git config --global user.email

# 使用 GitHub CLI 登錄
gh auth login

# 或配置 Personal Access Token
git config --global credential.helper manager-core
```

### 問題 2: 推送時提示大文件錯誤

**解決方案**:
```powershell
# 檢查是否有大文件被追蹤
git ls-files | ForEach-Object { Get-Item $_ -ErrorAction SilentlyContinue } | Where-Object { $_.Length -gt 100MB } | Select-Object FullName, Length

# 如果有，從 Git 中移除（但保留本地文件）
git rm --cached <大文件路徑>
git commit -m "Remove large files from Git"
```

### 問題 3: 遠程倉庫不為空

如果遠程倉庫已有內容（如 README），需要先拉取：

```powershell
# 拉取遠程內容並合併
git pull origin main --allow-unrelated-histories

# 解決可能的衝突後
git push -u origin main
```

### 問題 4: 分支名稱不匹配

如果本地分支不是 `main`：

```powershell
# 重命名分支
git branch -M main

# 或推送到當前分支，然後在 GitHub 設置默認分支
git push -u origin $(git branch --show-current)
```

---

## ✅ 上傳完成後

### 立即檢查事項

- [ ] 訪問倉庫頁面確認文件已上傳
- [ ] 檢查倉庫大小是否合理
- [ ] 確認 `.gitignore` 生效（不應有大文件）
- [ ] 檢查 GitHub Actions 是否正常運行（如果配置了 CI）

### 後續操作

1. **設置倉庫描述和標籤**
   - 在倉庫頁面點擊 "Settings"
   - 添加描述、網站、主題等

2. **設置分支保護規則**（可選）
   - Settings → Branches
   - 設置 `main` 分支保護規則

3. **配置 GitHub Actions Secrets**（如果需要）
   - Settings → Secrets and variables → Actions
   - 添加必要的環境變量

4. **設置 GitHub Pages**（如果需要文檔站點）
   - Settings → Pages
   - 選擇分支和路徑

---

## 📝 上傳命令總結

### 完整手動流程

```powershell
# 1. 檢查狀態
git status

# 2. 添加所有文件
git add .

# 3. 提交（如果需要）
git commit -m "chore: 準備上傳到新倉庫"

# 4. 設置遠程倉庫
git remote add origin https://github.com/victor2025PH/liaotianai1201.git
# 或更新現有遠程：git remote set-url origin https://github.com/victor2025PH/liaotianai1201.git

# 5. 推送到遠程
git push -u origin main
# 或推送到當前分支：git push -u origin $(git branch --show-current)
```

---

**完成時間**: 2025-11-30  
**下一步**: 執行上傳腳本或手動命令，然後驗證上傳結果
