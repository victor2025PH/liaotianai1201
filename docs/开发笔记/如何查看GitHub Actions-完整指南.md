# 如何查看 GitHub Actions - 完整指南

> **日期**: 2025-11-30  
> **問題**: 找不到 GitHub Actions 頁面

---

## 🎯 重要發現

**您的 CI 工作流配置為只在 `main` 或 `develop` 分支觸發！**

您推送的分支是 `test/cicd-validation`，**不會觸發 CI 工作流**。

---

## 📍 如何找到 GitHub Actions 頁面

### 步驟 1: 進入倉庫頁面

從您當前的 GitHub Dashboard 頁面：

1. **點擊左上角 GitHub Logo** 或
2. **點擊左側 "Top repositories" 列表中的您的倉庫** 或
3. **點擊右上角頭像 → "Repositories" → 找到您的倉庫**

### 步驟 2: 點擊 Actions 標籤

進入倉庫頁面後，在**頂部標籤欄**會看到：

```
┌─────────────────────────────────────────────────────┐
│ [Code] [Issues] [Pull requests] [Actions] [Projects]│
│                           ^^^^^^^^                    │
│                           點擊這裡！                   │
└─────────────────────────────────────────────────────┘
```

**Actions 標籤就在這裡！**

### 步驟 3: 查看工作流

點擊 "Actions" 後，您會看到：
- 左側：工作流列表（CI, Test Coverage, 等）
- 中間：工作流運行歷史

---

## ⚠️ 為什麼看不到您的測試運行？

### 原因分析

查看您的 CI 配置（`.github/workflows/ci.yml`）：

```yaml
on:
  push:
    branches: [ main, develop ]  # ← 只在這兩個分支觸發
  pull_request:
    branches: [ main, develop ]  # ← PR 到這兩個分支才觸發
```

**您推送到的分支**: `test/cicd-validation`  
**觸發條件**: 只在 `main` 或 `develop`  
**結果**: ❌ **不會觸發 CI 工作流**

---

## ✅ 解決方案：觸發 CI 工作流

### 方案 1: 推送到 develop 分支（推薦）⭐

```bash
# 切換到 develop 分支
git checkout develop

# 合併測試分支
git merge test/cicd-validation

# 推送（這會觸發 CI）
git push origin develop
```

**或者直接推送到 develop**:

```bash
# 切換到 develop
git checkout develop

# 做一個小的變更
echo "# CI/CD 測試 $(date)" >> test-cicd.md

# 提交並推送（會觸發 CI）
git add test-cicd.md
git commit -m "test: CI/CD 流程驗證"
git push origin develop
```

### 方案 2: 創建 Pull Request

這會觸發 CI 工作流：

```bash
# 確保代碼已推送到測試分支（已完成）
git push origin test/cicd-validation

# 然後在 GitHub 上創建 PR：
# 1. 訪問倉庫頁面
# 2. 點擊 "Pull requests" 標籤
# 3. 點擊 "New pull request"
# 4. 選擇 base: develop ← compare: test/cicd-validation
# 5. 創建 PR
```

### 方案 3: 修改 CI 配置（允許所有分支）

如果您想讓所有分支都觸發 CI，可以修改配置：

```yaml
on:
  push:
    branches: [ '*' ]  # 所有分支
  pull_request:
    branches: [ '*' ]  # 所有分支的 PR
```

---

## 🔍 查看 GitHub Actions 的步驟

### 完整路徑

1. **打開瀏覽器**
2. **訪問 GitHub 倉庫**
   - URL 格式：`https://github.com/victor2025PH/<倉庫名>`
3. **點擊頂部 "Actions" 標籤**
4. **查看工作流列表**

### 具體步驟（圖解）

```
GitHub Dashboard
    ↓
點擊左側 "Top repositories" 中的您的倉庫
    ↓
進入倉庫頁面
    ↓
看到頂部標籤：Code | Issues | Pull requests | Actions | ...
                                          ^^^^^^^^
                                          點擊這裡！
    ↓
進入 Actions 頁面
    ↓
查看工作流運行歷史
```

---

## 📊 Actions 頁面說明

進入 Actions 頁面後，您會看到：

### 左側欄

```
All workflows
  ├─ CI                    ← 主 CI 工作流
  ├─ Test Coverage         ← 測試覆蓋率
  ├─ Code Quality          ← 代碼質量
  ├─ Performance Test      ← 性能測試（新增）
  ├─ Lint and Fix          ← 自動修復（新增）
  └─ ... 其他工作流
```

### 中間區域

顯示每個工作流的運行歷史：
- ✅ 綠色 ✓ = 成功
- ❌ 紅色 × = 失敗
- 🟡 黃色 ● = 運行中

### 點擊具體運行查看詳情

```
CI 工作流運行 #123
├─ ✅ 後端代碼檢查
├─ ✅ 後端測試
│   └─ 檢查覆蓋率閾值: ✅ 通過（覆蓋率 75%）
├─ ✅ 前端代碼檢查
├─ ✅ 前端構建
├─ ✅ 前端 E2E 測試
└─ ✅ 綜合檢查
    └─ 查看報告 → 點擊查看詳細結果
```

---

## 🚀 立即測試：觸發 CI 工作流

### 快速命令

```bash
# 方法 1: 推送到 develop 分支（最簡單）
git checkout develop
echo "# CI/CD 測試 $(date)" >> test-cicd-develop.md
git add test-cicd-develop.md
git commit -m "test: CI/CD 流程驗證 - develop 分支"
git push origin develop

# 方法 2: 創建 PR（推薦，可以看到自動修復等功能）
# 代碼已經在 test/cicd-validation 分支，直接在 GitHub 創建 PR 即可
```

---

## 📝 檢查清單

### 找到 Actions 頁面
- [ ] 進入倉庫頁面（不是 Dashboard）
- [ ] 找到頂部 "Actions" 標籤
- [ ] 點擊進入 Actions 頁面

### 觸發 CI 工作流
- [ ] 確認推送到 `develop` 或 `main` 分支
- [ ] 或創建 Pull Request 到 `develop` 或 `main`
- [ ] 等待工作流運行（約 5-10 分鐘）

### 查看結果
- [ ] 在 Actions 頁面找到 "CI" 工作流
- [ ] 找到您的提交運行記錄
- [ ] 點擊查看詳細日誌

---

## 💡 快速訪問鏈接

如果您知道倉庫名稱，可以直接訪問：

### Actions 頁面
```
https://github.com/victor2025PH/<倉庫名>/actions
```

### 特定工作流
```
https://github.com/victor2025PH/<倉庫名>/actions/workflows/ci.yml
```

---

## ❓ 如果還是找不到

請檢查：

1. **確認倉庫名稱**
   ```bash
   # 查看 git 配置
   git remote -v
   # 或
   git config --get remote.origin.url
   ```

2. **確認 GitHub 用戶名**
   - 您的用戶名是：`victor2025PH`（從截圖看到）

3. **直接訪問**
   - 訪問：`https://github.com/victor2025PH`
   - 點擊您的倉庫
   - 然後點擊 "Actions" 標籤

---

## ✅ 下一步行動

### 立即執行

```bash
# 推送到 develop 分支觸發 CI
git checkout develop
git merge test/cicd-validation
git push origin develop
```

然後：
1. 等待 1-2 分鐘讓工作流開始
2. 訪問倉庫頁面
3. 點擊 "Actions" 標籤
4. 查看 "CI" 工作流運行狀態

---

**完成時間**: 2025-11-30  
**下一步**: 推送到 develop 分支，然後在 Actions 頁面查看結果
