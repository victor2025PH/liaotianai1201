# 处理子模块（Submodules）

> **问题**: `hbwy20251220` 和 `tgmini20251220` 显示为 "modified content, untracked content"

---

## 🔍 问题分析

如果这些目录显示为子模块状态，有两种可能：

1. **它们是独立的 Git 仓库**（有自己的 `.git` 目录）
2. **它们被注册为 Git 子模块**

---

## ✅ 解决方案

### 方案 1: 将它们作为普通目录提交（推荐）

如果这些目录不是真正的子模块，只是有未跟踪的内容：

```bash
# 进入目录并添加所有文件
cd d:\telegram-ai-system

# 添加所有内容（包括未跟踪的文件）
git add hbwy20251220/
git add tgmini20251220/
git add aizkw20251219/

# 提交
git commit -m "feat: 添加三个网站项目文件"

# 推送
git push origin main
```

### 方案 2: 如果它们是独立的 Git 仓库

如果需要保留它们作为独立的仓库：

```bash
# 初始化子模块（如果还没有）
git submodule add <repository-url> hbwy20251220
git submodule add <repository-url> tgmini20251220

# 更新子模块
git submodule update --init --recursive

# 提交子模块引用
git commit -m "feat: 添加三个网站作为子模块"
```

### 方案 3: 移除子模块状态（如果不需要）

如果它们不应该是子模块：

```bash
# 移除子模块配置
git rm --cached hbwy20251220
git rm --cached tgmini20251220

# 删除 .gitmodules 中的条目（如果存在）
# 编辑 .gitmodules 文件，删除相关条目

# 删除子模块的 .git 目录
rm -rf hbwy20251220/.git
rm -rf tgmini20251220/.git

# 重新添加为普通目录
git add hbwy20251220/
git add tgmini20251220/

# 提交
git commit -m "feat: 将子模块转换为普通目录"
```

---

## 🚀 推荐操作

对于这三个网站项目，**推荐使用方案 1**，将它们作为普通目录提交：

```powershell
cd d:\telegram-ai-system

# 强制添加所有文件（包括未跟踪的）
git add -f hbwy20251220/
git add -f tgmini20251220/
git add -f aizkw20251219/

# 检查状态
git status

# 提交
git commit -m "feat: 添加三个网站项目到主仓库

- tgmini20251220: TON Mini App Studio
- hbwy20251220: RedEnvelope.fi
- aizkw20251219: Smart Control King"

# 推送
git push origin main
```

---

## ⚠️ 注意事项

1. **确保 .env.local 文件不被提交**:
   - 这些文件应该在 `.gitignore` 中
   - 检查每个目录的 `.gitignore`

2. **检查文件大小**:
   - 确保没有提交 `node_modules` 或其他大文件
   - 检查 `.gitignore` 配置

3. **验证提交**:
   ```bash
   git log --oneline -1
   git show --stat HEAD
   ```

---

## 📚 相关文档

- [三个网站部署指南](./DEPLOY_THREE_SITES.md)
- [三个网站部署总结](./THREE_SITES_DEPLOYMENT_SUMMARY.md)
