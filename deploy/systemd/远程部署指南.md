# 远程部署指南

## 🚀 从本地 Windows 部署到 Linux 服务器

### 方法 1: 使用 SCP 上传文件

```powershell
# 在 Windows PowerShell 中执行

# 1. 上传部署文件到服务器
scp -r deploy/systemd/* user@your-server:/tmp/smart-tg-deploy/

# 2. SSH 连接到服务器
ssh user@your-server

# 3. 在服务器上执行
cd /tmp/smart-tg-deploy
chmod +x auto_deploy.sh
sudo ./auto_deploy.sh
```

### 方法 2: 使用 WinSCP 或 FileZilla

1. 使用图形化工具上传 `deploy/systemd/` 目录到服务器
2. SSH 连接到服务器
3. 执行部署脚本

### 方法 3: 使用 Git（如果服务器有 Git）

```bash
# 在服务器上
cd /opt
git clone your-repo-url smart-tg
cd smart-tg/deploy/systemd
chmod +x auto_deploy.sh
sudo ./auto_deploy.sh
```

---

## 📋 部署前检查清单

### 在本地准备

- [ ] 确认服务器 IP 地址和 SSH 访问权限
- [ ] 确认服务器已安装 Python 3.11+ 和 Node.js 18+
- [ ] 确认服务器有 root 或 sudo 权限
- [ ] 准备环境变量配置文件

### 在服务器上准备

- [ ] 创建项目目录: `/opt/smart-tg`
- [ ] 上传后端代码到: `/opt/smart-tg/admin-backend`
- [ ] 上传前端代码到: `/opt/smart-tg/saas-demo`
- [ ] 配置环境变量文件

---

## 🔧 一键部署命令（服务器端）

```bash
#!/bin/bash
# 在服务器上执行此命令即可完成部署

# 下载部署脚本（如果通过 Git）
# 或上传文件后执行：

cd /path/to/deploy/systemd
chmod +x auto_deploy.sh check_deployment.sh
sudo ./auto_deploy.sh
```

---

## ✅ 部署后验证

```bash
# 1. 检查服务状态
sudo systemctl status smart-tg-backend
sudo systemctl status smart-tg-frontend

# 2. 检查健康端点
curl http://localhost:8000/health
curl http://localhost:3000

# 3. 检查日志
sudo journalctl -u smart-tg-backend -f
sudo journalctl -u smart-tg-frontend -f
```

---

*远程部署指南最后更新: 2025-11-26*

