# 401 错误 - 一键登录脚本

## 在浏览器控制台执行

打开浏览器控制台（按 `F12`），复制粘贴以下代码：

```javascript
// 一键登录脚本
fetch('http://aikz.usdt2026.cc/api/v1/auth/login', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/x-www-form-urlencoded',
  },
  body: new URLSearchParams({
    username: 'admin@example.com',
    password: 'changeme123'
  })
})
.then(res => {
  if (!res.ok) {
    throw new Error(`HTTP ${res.status}`)
  }
  return res.json()
})
.then(data => {
  localStorage.setItem('access_token', data.access_token)
  console.log('✅ 登录成功！Token已保存')
  console.log('🔄 正在刷新页面...')
  setTimeout(() => location.reload(), 500)
})
.catch(err => {
  console.error('❌ 登录失败:', err)
  alert('登录失败，请检查：\n1. 后端服务是否运行\n2. 网络连接是否正常\n\n错误: ' + err.message)
})
```

执行后：
- ✅ 自动登录
- ✅ Token 保存到 localStorage
- ✅ 自动刷新页面

## 方法2：访问登录页面

直接访问：
```
http://aikz.usdt2026.cc/login
```

登录页面会自动填充默认账号，只需点击"登录"即可。

## 默认管理员账号

- **邮箱**: `admin@example.com`
- **密码**: `changeme123`

## 验证

登录后，在控制台检查：

```javascript
console.log('Token:', localStorage.getItem('access_token') ? '✅ 存在' : '❌ 不存在')
```

---

**最简单：复制粘贴方法1的代码到浏览器控制台执行！**
