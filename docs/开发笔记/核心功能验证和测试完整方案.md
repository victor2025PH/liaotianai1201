# 核心功能驗證和測試完整方案

> **日期**: 2025-12-01  
> **步驟**: 3/3 - 核心功能測試

---

## 📋 已發現的核心模塊

- ✅ `group_ai_service/coordination_manager.py` - 協同管理器
- ✅ `group_ai_service/script_engine.py` - 劇本引擎  
- ✅ `group_ai_service/message_analyzer.py` - 消息分析器

---

## 🚀 步驟 1: 驗證模塊可以導入

在服務器終端執行：

```bash
cd ~/liaotian && \
echo "=== 步驟 1: 驗證模塊導入 ===" && \
python3 << 'EOF'
import sys
from pathlib import Path

project_root = Path.cwd()
sys.path.insert(0, str(project_root))

print("檢查核心功能模塊...\n")

modules = [
    ("group_ai_service.coordination_manager", "CoordinationManager", "協同管理器"),
    ("group_ai_service.script_engine", "ScriptEngine", "劇本引擎"),
    ("group_ai_service.message_analyzer", "MessageAnalyzer", "消息分析器"),
]

all_ok = True
for module_name, class_name, desc in modules:
    try:
        module = __import__(module_name, fromlist=[class_name])
        cls = getattr(module, class_name)
        print(f"✅ {desc} ({class_name}) - 可以導入")
    except Exception as e:
        print(f"❌ {desc} ({class_name}) - 導入失敗: {e}")
        all_ok = False

print("\n" + "="*50)
if all_ok:
    print("✅ 所有核心模塊都可以導入")
else:
    print("❌ 部分模塊導入失敗")
EOF
```

---

## 🚀 步驟 2: 執行簡化測試（如果有測試腳本）

```bash
cd ~/liaotian && \
if [ -f "scripts/test_core_features_simple.py" ]; then
    echo "=== 步驟 2: 執行簡化測試 ===" && \
    python3 scripts/test_core_features_simple.py
else
    echo "測試腳本不存在，跳過自動化測試"
fi
```

---

## 🚀 步驟 3: API 層面驗證

檢查相關 API 端點是否存在：

```bash
echo "=== 步驟 3: 檢查 API 端點 ===" && \
echo "" && \
echo "1. 檢查 API 文檔..." && \
curl -s http://localhost:8000/docs | grep -i "hot.*update\|script\|account" | head -5 && \
echo "" && \
echo "2. 檢查劇本 API..." && \
curl -s -o /dev/null -w "劇本列表 API: HTTP %{http_code}\n" http://localhost:8000/api/v1/group-ai/scripts/ && \
echo "3. 檢查賬號 API..." && \
curl -s -o /dev/null -w "賬號列表 API: HTTP %{http_code}\n" http://localhost:8000/api/v1/group-ai/accounts/ && \
echo "" && \
echo "=== API 檢查完成 ==="
```

---

## ✅ 完整驗證流程

執行以下完整驗證：

```bash
cd ~/liaotian && \
echo "=========================================" && \
echo "核心功能完整驗證" && \
echo "=========================================" && \
echo "" && \
echo "【步驟 1】模塊導入驗證..." && \
python3 << 'EOF'
import sys
from pathlib import Path
project_root = Path.cwd()
sys.path.insert(0, str(project_root))

modules = [
    ("group_ai_service.coordination_manager", "CoordinationManager", "協同管理器"),
    ("group_ai_service.script_engine", "ScriptEngine", "劇本引擎"),
    ("group_ai_service.message_analyzer", "MessageAnalyzer", "消息分析器"),
]

for module_name, class_name, desc in modules:
    try:
        module = __import__(module_name, fromlist=[class_name])
        cls = getattr(module, class_name)
        print(f"  ✅ {desc}")
    except Exception as e:
        print(f"  ❌ {desc}: {e}")
EOF
echo "" && \
echo "【步驟 2】API 端點檢查..." && \
echo "  劇本 API: $(curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/api/v1/group-ai/scripts/)" && \
echo "  賬號 API: $(curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/api/v1/group-ai/accounts/)" && \
echo "" && \
echo "=========================================" && \
echo "驗證完成" && \
echo "========================================="
```

---

## 📊 判斷標準

### ✅ 通過標準

- 所有核心模塊可以導入
- API 端點可以訪問（HTTP 200 或 401 表示端點存在）
- 沒有導入錯誤

### ⚠️ 需要進一步檢查

- 如果模塊導入失敗，需要檢查依賴或路徑問題
- 如果 API 返回 404，需要檢查路由配置

---

**執行完整驗證流程，告訴我結果！**
