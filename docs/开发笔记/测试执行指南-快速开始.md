# 測試執行指南 - 快速開始

> **日期**: 2025-11-30  
> **測試對象**: 4個核心功能

---

## 🚀 快速測試

### 第一步：檢查環境

```bash
# 1. 確保在項目根目錄
cd "e:\002-工作文件\重要程序\聊天AI群聊程序"

# 2. 檢查 Python 版本（需要 3.8+）
python --version

# 3. 檢查必要的模塊是否可以導入
python -c "from group_ai_service.coordination_manager import CoordinationManager; print('OK')"
python -c "from group_ai_service.message_analyzer import MessageAnalyzer; print('OK')"
```

### 第二步：運行單元測試

```bash
# 運行自動化測試腳本
python scripts/test_core_features.py
```

### 第三步：查看測試結果

測試會輸出：
- ✅ 每個功能的測試狀態
- 📊 總體測試結果
- ⚠️ 任何失敗的測試詳情

---

## 📋 測試清單

### ✅ 測試 1: 多賬號協同邏輯

**測試內容**:
- [ ] 協同管理器初始化
- [ ] 賬號註冊
- [ ] 優先級檢查
- [ ] 回復鎖機制

**預期結果**: 只有高優先級賬號回復

---

### ✅ 測試 2: 劇本熱更新

**測試內容**:
- [ ] 劇本解析
- [ ] 劇本初始化
- [ ] 熱更新方法
- [ ] 狀態保留

**預期結果**: 劇本更新成功，狀態保留

---

### ✅ 測試 3: 新成員檢測

**測試內容**:
- [ ] new_chat_members 屬性檢測
- [ ] service 類型檢測
- [ ] 普通消息不誤判

**預期結果**: 正確檢測新成員

---

### ✅ 測試 4: 多輪對話增強

**測試內容**:
- [ ] 意圖識別
- [ ] 話題檢測
- [ ] 情感分析
- [ ] 實體提取

**預期結果**: 所有分析功能正常

---

## 🔧 如果測試失敗

### 問題 1: 導入錯誤

**錯誤**: `ModuleNotFoundError: No module named 'group_ai_service'`

**解決**:
```bash
# 確保在項目根目錄
cd "e:\002-工作文件\重要程序\聊天AI群聊程序"

# 檢查 Python 路徑
python -c "import sys; print('\n'.join(sys.path))"
```

### 問題 2: 缺少依賴

**錯誤**: `ImportError: cannot import name 'X'`

**解決**:
```bash
# 檢查文件是否存在
ls group_ai_service/coordination_manager.py
ls group_ai_service/message_analyzer.py
```

---

## 📝 手動測試（如果自動化測試無法運行）

### 測試 1: 多賬號協同

```python
python -c "
from group_ai_service.coordination_manager import CoordinationManager, ReplyPriority
import asyncio

async def test():
    manager = CoordinationManager()
    await manager.start()
    manager.register_account_role('acc1', role_id='role1', priority=ReplyPriority.HIGH)
    print('✅ 協同管理器初始化成功')

asyncio.run(test())
"
```

### 測試 2: 消息分析器

```python
python -c "
from group_ai_service.message_analyzer import MessageAnalyzer

analyzer = MessageAnalyzer()
print('✅ 消息分析器初始化成功')
print(f'支持意圖: {list(analyzer.intent_keywords.keys())}')
print(f'支持話題: {list(analyzer.topic_keywords.keys())}')
"
```

---

## 📊 測試結果記錄

測試完成後，請記錄：

```
測試時間: 2025-11-30
測試環境: Windows / Linux
Python 版本: 3.x

測試結果:
1. 多賬號協同邏輯: ✅ / ❌
2. 劇本熱更新: ✅ / ❌
3. 新成員檢測: ✅ / ❌
4. 多輪對話增強: ✅ / ❌

問題記錄:
- ...
```

---

## 📚 更多信息

詳細測試指南請查看：
- `docs/开发笔记/核心功能测试指南.md` - 完整測試指南
- `scripts/test_core_features.py` - 測試腳本源碼

---

**快速指南版本**: v1.0  
**最後更新**: 2025-11-30
