# 快速測試檢查清單

> **更新日期**: 2025-01-07

---

## 🚀 服務啟動檢查

- [ ] 後端服務運行在 `http://localhost:8000`
- [ ] 前端服務運行在 `http://localhost:3000`
- [ ] 可以訪問後端 API 文檔：`http://localhost:8000/docs`
- [ ] 可以訪問前端界面：`http://localhost:3000`

---

## 📋 核心功能測試

### 1. 劇本管理
- [ ] 訪問 `http://localhost:3000/group-ai/scripts`
- [ ] 創建新劇本
- [ ] 編輯劇本
- [ ] 測試劇本解析

### 2. 帳號管理
- [ ] 訪問 `http://localhost:3000/group-ai/accounts`
- [ ] 添加帳號（需要有效的 session 文件）
- [ ] 啟動帳號
- [ ] 檢查帳號狀態變為 ONLINE

### 3. 劇本和帳號聊天功能
- [ ] 帳號啟動後，檢查日誌確認：
  - [ ] 劇本已加載
  - [ ] 劇本引擎已初始化
  - [ ] 對話管理器已初始化
  - [ ] 會話池已啟動消息監聽
- [ ] 在 Telegram 群組中發送測試消息
- [ ] 驗證帳號自動回復

### 4. 角色分配功能
- [ ] 訪問 `http://localhost:3000/group-ai/role-assignments`
- [ ] 提取角色（需要包含角色定義的劇本）
- [ ] 創建自動分配方案
- [ ] 創建手動分配方案
- [ ] 審查分配方案
- [ ] 應用分配方案

---

## 🔍 API 測試

### 測試劇本 API
```bash
# 獲取劇本列表
curl http://localhost:8000/api/v1/group-ai/scripts/

# 創建劇本
curl -X POST http://localhost:8000/api/v1/group-ai/scripts/ \
  -H "Content-Type: application/json" \
  -d '{"script_id": "test", "name": "測試", "version": "1.0", "yaml_content": "..."}'
```

### 測試帳號 API
```bash
# 獲取帳號列表
curl http://localhost:8000/api/v1/group-ai/accounts/

# 啟動帳號
curl -X POST http://localhost:8000/api/v1/group-ai/accounts/test_account_001/start
```

### 測試角色分配 API
```bash
# 提取角色
curl -X POST http://localhost:8000/api/v1/group-ai/role-assignments/extract-roles \
  -H "Content-Type: application/json" \
  -d '{"script_id": "test_script"}'

# 創建分配方案
curl -X POST http://localhost:8000/api/v1/group-ai/role-assignments/create-assignment \
  -H "Content-Type: application/json" \
  -d '{"script_id": "test_script", "account_ids": ["account_001"], "mode": "auto"}'
```

---

## ⚠️ 常見問題

### 後端服務無法啟動
- 檢查 Python 依賴是否安裝：`cd admin-backend && pip install -r requirements.txt`
- 檢查數據庫連接配置
- 查看後端日誌錯誤信息

### 前端服務無法啟動
- 檢查 Node.js 依賴：`cd saas-demo && npm install`
- 檢查端口 3000 是否被占用

### 帳號無法啟動
- 檢查 session 文件是否存在
- 檢查 TELEGRAM_API_ID 和 TELEGRAM_API_HASH 配置
- 查看後端日誌

### 無法提取角色
- 確保劇本包含角色定義（metadata.roles）
- 檢查劇本格式是否正確

---

## 📝 測試記錄

**測試日期**: ___________

**測試人員**: ___________

**測試結果**:
- 劇本管理: ✅ / ❌
- 帳號管理: ✅ / ❌
- 聊天功能: ✅ / ❌
- 角色分配: ✅ / ❌

**發現的問題**:
1. 
2. 

**備註**:
_________________________________

---

**最後更新**: 2025-01-07

